{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
      
<main class="mx-lg-5">
    <div class="container-fluid mt-5">
        <div class="container"></div>
            <div class="row">
                <div class="col-md-12">
                    <h1>Catégorie</h1>

                        <table class="table">
                            <tbody>
                                <tr>
                                    <th>Id</th>
                                    <td>{{ categorie.id }}</td>
                                </tr>
                                <tr>
                                    <th>Nom</th>
                                    <td>{{ categorie.nom }}</td>
                                </tr>
                            </tbody>
                        </table>
                        <button type="button" class="btn"><a href="{{ path('categorie_edit', {'id': categorie.id}) }}" style="color: black;">Modifier</a></button>

                        <button type="button" class="btn btn-dark"><a href="{{ path('categorie_index') }}" style="color: white;">Retour vers le menu</a></button>

                        {{ include('categorie/_delete_form.html.twig') }}
                </div>
            </div>  
        </div>
    </div>
</main>
    
{% endblock %}
